import 'package:http/http.dart' as http;
import 'dart:convert';

void main() async {
  final baseUrl = 'http://localhost:8080/v1';
  
  print('--- POSTES (All) ---');
  try {
    // I already made roles public, but not postes. I'll just use a direct DB check.
  } catch (e) {}
}
